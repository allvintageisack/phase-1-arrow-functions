// making a function to divide variables 
const divide = (a, b) => a / b;
divide(2000, 100)
//squaring 
const square = (a) => a * a;
square(2)

//adding function
const add = (x, y) => x + y;
add(3, 4)